# Prerequisite

```
python -m venv .venv

source .venv/bin/activate

pip install -e .

cd ..

pip install -e .

cd ui/frontend

npm install -g gatsby-cli

npm install --global yarn

yarn install

yarn build
```

# Configure

# Run Web UI

```
cd frontend

./run.sh 
```
